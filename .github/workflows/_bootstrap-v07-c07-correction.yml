name: Retired V0.7 C-07 correction bootstrap

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Bootstrap retired after the approved correction was applied.'
