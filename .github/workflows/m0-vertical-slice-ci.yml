name: M0 vertical slice CI

on:
  pull_request:
  push:
    branches:
      - main
    paths:
      - "host/simulator/**"
      - "host/greenhouse-manager/**"
      - "infra/compose/m0/**"
      - "infra/mosquitto/dev/**"
      - "protocols/mqtt/**"
      - "protocols/pairing/**"
      - ".github/workflows/m0-vertical-slice-ci.yml"

permissions:
  contents: read

jobs:
  scope:
    runs-on: ubuntu-24.04
    outputs:
      run: ${{ steps.detect.outputs.run }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - id: detect
        env:
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          set -euo pipefail
          if [[ "$EVENT_NAME" != "pull_request" ]]; then
            echo "run=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          git diff --name-only "$BASE_SHA" "$HEAD_SHA" > "$RUNNER_TEMP/changed-files.txt"
          if grep -Eq '^(host/simulator/|host/greenhouse-manager/|infra/compose/m0/|infra/mosquitto/dev/|protocols/mqtt/|protocols/pairing/|\.github/workflows/m0-vertical-slice-ci\.yml$)' "$RUNNER_TEMP/changed-files.txt"; then
            echo "run=true" >> "$GITHUB_OUTPUT"
          else
            echo "run=false" >> "$GITHUB_OUTPUT"
          fi

  simulator-unit:
    needs: scope
    if: needs.scope.outputs.run == 'true'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: host/simulator

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: host/simulator/pyproject.toml

      - name: Install
        run: python -m pip install -e '.[dev]'

      - name: Lint
        run: ruff check .

      - name: Test
        run: pytest -q

  compose-integration:
    needs: scope
    if: needs.scope.outputs.run == 'true'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Validate Compose
        run: docker compose -f infra/compose/m0/docker-compose.yml config

      - name: Verify MQTT vertical slice
        run: bash infra/compose/m0/verify.sh