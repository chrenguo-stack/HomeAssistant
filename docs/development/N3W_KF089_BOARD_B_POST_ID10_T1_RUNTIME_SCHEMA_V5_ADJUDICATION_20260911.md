# N3W KF-089 Board B post-ID10 T1 runtime / Schema-v5 adjudication — 2026-09-11

## Live T1 evidence

The read-only T1/Broker correlation after the operator-confirmed ID10 full power cycle returned:

```text
T1_REACHABLE=true
BROKER_RUNNING=true
MANAGER_RUNNING=true
ID10_TIME_RECOVERED=false
BOARD_B_FRESH_TELEMETRY_FOUND=true
POST_ID10_CORRELATION=PASS_WINDOWED_ANCHOR_ONLY
FIRST_TELEMETRY_TIME=2026-09-11T16:04:04.754Z
LAST_TELEMETRY_TIME=2026-09-11T16:20:09.888Z
TELEMETRY_COUNT=194
SEQ_RANGE=0-193
DIRECT_COUNT=194
RELAY_COUNT=0
OTHER_INGRESS_COUNT=0
ACCEPTED_COUNT=194
PRODUCT_RUNTIME_AFTER_POWER_CYCLE=PASS
```

Because the exact ID10 wall-clock execution time was not recovered, the temporal correlation remains `PASS_WINDOWED_ANCHOR_ONLY`; no stronger exact timestamp claim is made.

## Exact source contract

The deployed Phase-4 generic harness source authority enables:

```text
phase4_product_runtime: true
phase4_lab_diagnostics: true
```

Its 5-second synthetic telemetry path returns immediately unless `runtime_ready()` is true. It then obtains a product-owned boot/sequence identity and calls `send_telemetry_json(...)`.

In the exact product component, `runtime_ready_` is set true only after the N3-W radio initializes and `runtime_.start(...)` returns success. Therefore accepted Phase-4 telemetry generated by this exact harness is positive evidence that the N3-W product runtime has successfully started.

The same telemetry-identity path calls `bind_lab_diagnostic_boot_session_(...)` once the boot-session manager is ready. The enabled `N3wLabDiagnostics::Snapshot` has compile-time `kSchemaVersion = 5`. Therefore successful post-boot Phase-4 telemetry with issued sequence numbers also establishes that the enabled lab diagnostics are bound to a live boot session using Schema v5.

## Adjudication

```text
PRODUCT_RUNTIME_AFTER_POWER_CYCLE=PASS
N3W_RUNTIME=PASS
SCHEMA_V5_RUNTIME_STATE=PASS
N3W_DIAG_DISCOVERY_SERIAL_LINE=NOT_DIRECTLY_OBSERVED
BOOT_SLOT_APP0=DIRECTLY_NOT_OBSERVED
FIRMWARE_FAILURE_PROVEN=false
APP0_REWRITE_REQUIRED=false
OTADATA_REWRITE_REQUIRED=false
```

`N3W_RUNTIME=PASS` and `SCHEMA_V5_RUNTIME_STATE=PASS` are not inferred merely from ordinary MQTT connectivity. They are bound to the exact deployed Phase-4 harness contract and the live sequence-bearing accepted telemetry stream.

The literal USB log line `N3W_DIAG_DISCOVERY schema=5 ...` was not captured during ID10 because the observer lost the USB target across the power cycle. That missing serial line is retained as an observation gap, not as a runtime failure.

## Route consequence

Board B recovery is sufficient to leave the boot-recovery detour. Do not rewrite app0, otadata, or NVS and do not repeat the power cycle merely to recapture a serial line.

Return to the product-level unresolved relay gate:

```text
FIRST_UNPROVEN_STAGE=B_UNICAST_TX_COMPLETION_OR_A_COMPACT_RX
KF089_END_TO_END_RELAY_TELEMETRY=NOT_PROVEN
```
