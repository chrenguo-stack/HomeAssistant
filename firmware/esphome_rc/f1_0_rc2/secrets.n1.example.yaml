# Copy this file to secrets.yaml for N1 builds, then replace the broker IP
# with the LAN address of the T1 host. 192.0.2.1 is a documentation-only
# address and will never connect to a real broker.
n1_mqtt_broker: 192.0.2.1
n1_mqtt_port: 1883
